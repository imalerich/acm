import java.util.ArrayList;

public class Arithmetic {

}

class ArithmeticSet<T> {
	ArrayList<T> values;
	
	ArithmeticSet(T First, T Second, T Third, T Fourth) {
		values = new ArrayList<T>();
		values.add(First);
		values.add(Second);
		values.add(Third);
		values.add(Fourth);
	}
	
	ArrayList<ArithmeticSet> Permutations() {
		ArrayList<ArithmeticSet<T>> out = new ArrayList<ArithmeticSet>();
		for (int i=0; i<values.size(); i++) {
			for (int j=0; j<values.size(); j++) {
				if (i != j) {
					
				}
			}
		}
		
		return out;
	}
}